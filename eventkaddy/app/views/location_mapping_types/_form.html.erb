<%= form_for(@location_mapping_type) do |f| %>
  <% if @location_mapping_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_mapping_type.errors.count, "error") %> prohibited this location_mapping_type from being saved:</h2>

      <ul>
      <% @location_mapping_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_name %>
    <%= f.text_field :type_name %><br /><br />
  </div>
  <div class="actions">
    <%= f.submit class:"btn btn-primary"   %>
  </div>
<% end %>
