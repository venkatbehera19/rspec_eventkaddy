<%= form_for(@record_type) do |f| %>
  <% if @record_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@record_type.errors.count, "error") %> prohibited this record_type from being saved:</h2>

      <ul>
      <% @record_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :record_type %>
    <%= f.text_field :record_type %><br /><br />
  </div>
  <div class="actions">
    <%= f.submit class:"btn btn-primary"   %>
  </div>
<% end %>
