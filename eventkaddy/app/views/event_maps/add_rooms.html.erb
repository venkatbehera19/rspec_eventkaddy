<%= form_for(@event_map, :html => {:class=>"form-horizontal well" }, :url => {:action => "update_add_rooms"} ) do |f| %>
  <% if @event_map.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_map.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event_map.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <% if @location_mappings!=nil %>
    <div class="form-group">
      <%= label_tag(:add_location_mappings, "Select Locations:") %>
        <%= select_tag(:location_mapping_ids, options_for_select(@location_mappings.map { |location_mapping| ["#{location_mapping.name}" ,location_mapping.id] },@location_mappings),:multiple => true, :size=>30, class: "custom-select") %>
    </div>

  <% else %>
    <div class="form-group">
      <%= label_tag(:add_location_mappings, "Select Locations:") %>
        <%= select_tag(:location_mapping_ids, options_for_select(@location_mappings.map { |location_mappingsping| ["#{location_mapping.name}" ,location_mapping.id] }),:multiple => true, :size=>30, class: "custom-select") %>
    </div>
  <% end %>

   <div class="actions">
    <%= f.submit "Add Locations to Map",class:"btn btn-primary"   %>
  </div>
<% end %>