<%= render partial: 'new', locals: { user: @user, submit_path: two_factor_settings_path(id: @user.id), submit_button_value: "Confirm and Enable Two Factor", headingTwoFA: false, set_remote: false } %>
