<!DOCTYPE html>
<html>
  <head>

  <meta charset="utf-8">
  <title>Forgot Password?</title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>

  <style>
    .alert-note {
      color: #998728;
      background-color: #F2F1AE;
      border-color: #787404;
      border-radius: 5px;
    }

    .normal-link-style {
      color:blue;
    }

    .normal-link-style:hover {
      color:#010041;
      background-color:transparent;
      text-decoration: underline;
      cursor:pointer;
    }

    #get_reg_id_box {
      position:fixed;
      text-align      : center;
      border-radius   : 10px;
      /*padding-top     : 20px;*/
      max-height          : 300px;
      max-width           : 400px;
      background-color: white;
      z-index         : 19;
      color           : black;
    }

  </style>

  <body>
    <center>This page is no longer active. Please look for the forgot-password link on the sign in page of your event's application instead.</center>
<!--     <div id="exprod" class="container-fluid center-block" style="width:300px;height:100%;">

      <center>

        <div>
          <h4>Retrieve Password</h4>
          Email<br>
          <input id="rid-email"></input><br><br>

          <div id="get-pass" class="btn btn-primary">Retrieve Password</div>
        </div>
      </center>
    </div> -->

    <script>

      // function getAttendeeRegId() {

      //   var email = $('#rid-email').val();

      //   var send_data  = { email:email, event_id:64};

      //   $.ajaxSetup({ timeout: 10000 });

      //   $.ajax({
      //     type       : 'GET',
      //     url        : '/forgot_password/retrieve_password',
      //     // dataType: 'JSON',
      //     data       : send_data,
      //     success    : function(srv_data) {
      //       response = srv_data;
      //       alert(response[0].message);
      //     },
      //     error: function(jqXHR, textStatus, errorThrown) {
      //       if (debug) {
      //         console.log("ajax error");
      //         console.log(jqXHR);
      //         console.log(textStatus);
      //         console.log(errorThrown);
      //       }
      //     }
      //   });
      // }

      // var debug = false;

      // $('#get-pass').on('click touchstart', function(e) {
      //   e.preventDefault();
      //   getAttendeeRegId();
      // });

    </script>

  </body>

</html>